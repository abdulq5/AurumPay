spring:
  config:
    activate:
      on-profile: postgres
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:aurumpay}
    username: ${DB_USER:aurumpay}
    password: ${DB_PASSWORD:aurumpay}
  flyway:
    locations: classpath:db/migration